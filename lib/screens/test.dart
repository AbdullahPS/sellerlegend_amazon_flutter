import 'package:flutter/material.dart';

class Test extends StatelessWidget {
  static const routePathName = 'Test';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('TEST'),
    );
  }
}
